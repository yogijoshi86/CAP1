sap.ui.define([
    "sap/ui/test/opaQunit"
], function (opaTest) {
    "use strict";

    var Journey = {
        run: function() {
            QUnit.module("First journey");

            opaTest("Start application", function (Given, When, Then) {
                Given.iStartMyApp();

                Then.onThetime_attendance_reportingList.iSeeThisPage();

            });


            opaTest("Navigate to ObjectPage", function (Given, When, Then) {
                // Note: this test will fail if the ListReport page doesn't show any data
                
                When.onThetime_attendance_reportingList.onFilterBar().iExecuteSearch();
                
                Then.onThetime_attendance_reportingList.onTable().iCheckRows();

                When.onThetime_attendance_reportingList.onTable().iPressRow(0);
                Then.onThetime_attendance_reportingObjectPage.iSeeThisPage();

            });

            opaTest("Teardown", function (Given, When, Then) { 
                // Cleanup
                Given.iTearDownMyApp();
            });
        }
    }

    return Journey;
});